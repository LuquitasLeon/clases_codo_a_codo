function sumar(nro1, nro2) {
  return (nro1 + nro2)
}

function restar(nro1, nro2) {
  return (nro1 - nro2)
}

function multiplicar(nro1, nro2) {
  return (nro1 * nro2)
}

function dividir(nro1, nro2) {
  return (nro1 / nro2)
}

// Parametros: les doy un nombre a las variables que almacenaran temporalmente unos valores que les pasare cuando invoque a la funcion.

 let resultadoSuma = "La suma es " + sumar(10, 20);
resultadoSuma

sumar(200, 500)
restar(300, 100)
sumar(249, 657)