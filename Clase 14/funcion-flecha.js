// funciones flecha

/*
const sumar = function () {
  return 2 + 2
}
*/

// Desaparece la palabra function y el return es implicito si esta en una misma linea
const sumar = () => 2 + 2

// Si hay un solo parametro, no es necesario indicar parentesis
const restar = nro1 => nro1 - 100